import Main from "@/components/Main";
import { Dot } from "lucide-react";
export default  function Home() {

  // Math.floor(Math.random() * primaryImage.length)),
  // Math.floor(Math.random() * primaryImage.length))

  return (
    <>
      <div>

        <Main></Main>
      </div>
    </>
  );
}
